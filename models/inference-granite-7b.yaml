# granite-7b — catalog/registry-native storage (HF or OCI); swap uri for your Model Registry or catalog ModelCar.
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: granite-7b
  namespace: rag-demo
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  predictor:
    model:
      modelFormat:
        name: huggingface
      runtime: vllm-gpu-runtime
      storage:
        uri: hf://ibm-granite/granite-7b-instruct
    minReplicas: 1
    maxReplicas: 1
    resources:
      limits:
        nvidia.com/gpu: "1"
        memory: "24Gi"
        cpu: "4"
      requests:
        nvidia.com/gpu: "1"
        memory: "20Gi"
        cpu: "2"
    nodeSelector:
      nvidia.com/gpu.product: NVIDIA-H100-80GB-HBM3
    tolerations:
      - key: "nvidia.com/gpu"
        operator: Exists
        effect: NoSchedule
