apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - app-of-apps.yaml
  # Optional: ArgoCD resource customizations for health (apply to argocd-cm)
  # - resource-customizations.yaml
